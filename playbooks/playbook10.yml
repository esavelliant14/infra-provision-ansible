---

- name: Playbook 10 memisahkan variable host var dan group var beda file
  hosts: server
  tasks:
    - name: task 1
      shell: echo "ini {{ os }}" > /home/automate/os.txt

    - name: task 2
      shell: echo "ini {{ aa }}" >> /home/automate/os.txt
